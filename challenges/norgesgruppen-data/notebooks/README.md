# Notebooks

## Canonical Notebook

- `05_colab_l4_g4_optimized_pipeline.ipynb`
  - Stable Colab notebook for the final training and export workflow.
  - Self-contained bootstrap by default (`CODE_SOURCE='embedded'`).
  - Produces the final submission candidate and its accountability bundle in external storage.
  - Runs lineage verification and records artifact metadata for later review.

Only the canonical notebook needed for evaluator review is included in the submission tree.
